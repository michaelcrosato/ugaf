# feedback/0001 — Loop B, round 1 (self-playtest swarm)

> Source: `npm run playtest` — 4 player personas (curious-methodical, reckless, social-buyer, wanderer)
> played The Hush through PROCTOR; every session realness-VERIFIED (nonce chain + wall-clock + replay).
> This is the first turn of the flywheel: play → observe → fix → re-verify.

## Signal (what the transcripts showed)

| # | Finding | Severity | Area | Status |
|---|---|---|---|---|
| 1 | A winning run was reported as `final=active` — PROCTOR read kernel `state.status` ('active') instead of the Session's goal outcome ('won'). | P0 | system | **fixed** (Session.outcome(); ProctorSession uses it) |
| 2 | `bribe mox` was rejected: `social.fate may not write fact namespace "objective"` — the Mox law-map sets `objective.cache_route`. | P0 | system | **fixed** (social writesFacts += objective) |
| 3 | Typing `survey` / `salvage` (exit names) hit the same-spelled VERB (`survey` a law) instead of walking to the Survey / Striders. | P1 | parser | **fixed** (a single token naming an exit HERE wins over a same-spelled verb) |
| 4 | `bribe eun` when Eun was not present silently retargeted to whoever WAS there (Old Lyle). | P1 | parser | **fixed** (a named-but-absent NPC is rejected, not silently retargeted) |
| 5 | No generic `back` / `out` / `leave` / `return` — reasonable "leave" inputs failed on nodes lacking that exit dir; reject messages were terse. | P1 | ux | **fixed** (return-words retrace your steps; rejects now list the real exits) |
| 6 | Prose nits: "You take core", "There is core here". | P2 | prose | **fixed** (articles) |

## What is already working (do not regress)

- **Fairness holds.** The reckless player who shouted a name at the Antenna Field got the **non-lethal warning
  first** ("You are unhurt. You will not be, twice."), and only died on the **second** name — exactly the
  warn-before-kill contract. The death epilogue lands.
- **Two honest solution paths both win:** anomaly-timing (curious-methodical, day-march) and social-buy
  (social-buyer), plus the unit-tested non-metal path = the demo's ≥3 independent paths.
- **The learning loop is satisfying:** examine a tell → witness the law → deduce to `surveyed` in the codex.
- **Determinism + realness:** all four sessions replay bit-for-bit and pass the realness oracle.

## Result of this round

5 P0/P1 findings closed; 3/4 personas now play clean (the wanderer is a deliberate parser-stress run).
Gate green, golden tape re-pinned (30-turn winning canonical playthrough). The deeper-quality critique
(fun/dwell, fairness, prose, balance, replay) is round 2 → feedback/0002.

# feedback/0003 — Loop B round 2 closure (what round 2 consumed)

> Loop A's response to feedback/0002 (the 42-finding deep critique), across three sub-cycles 2a/2b/2c,
> all gate-passing. This file closes the loop: it records which 0002 Top-fixes are DONE and what is
> deliberately deferred to a future cycle. The flywheel turned three times this session.

## Closed (verified in tests + playtest)

| 0002 # | Fix | Where | Verified |
|---|---|---|---|
| 1 | **Greywater bites on the optimal path** — "after dark" begins at DUSK; a naive march lands in the law's teeth (first contact warns). | `laws.ts`, `world.ts` dusk variants | the "minutes, not hours" tell now fires in every marching transcript |
| 2 | **Codex / inventory / map / recall reachable via PROCTOR** (not just the CLI), no turn burned. | `parse.ts` META, `session.ts` applyIntent | meta-intents intercepted before kernel routing |
| 3 | **Deduction pays, buying costs** — bought knowledge enters the codex as "(from a bought map — unverified)". | `npcs.ts`, `session.ts` codex | buyer's codex shows the unverified flag |
| 4 | **Branching endings** — "Read True" (mastery) vs "on a Bought Map" vs nerve-not-knowing; the Striders remember the debt. | `index.ts` goals | the four personas now reach DISTINCT epilogues |
| 5 | **Per-seed law variance** — a 4th law (The Hollow Dark) is the rotating slot; ~half of seeds surface a never-seen law. | `laws.ts`, `tells.ts`, `index.ts` | variance test: live on some seeds, absent on others |
| 6 | **Law Drift fires** — dwell-based (driftAfter turns since survey), so mastery refreshes on its own clock; warn strictly before demote. | `law.ts`, `anomaly.ts`, `invest.ts` | dwell-drift test: warn→demote relative to mastery |
| 7 | **Antenna escape window** — first contact warns + the Changed hunts; LEAVING the field escapes, speaking again or lingering is lethal. Auto-teaches the name-stones on arrival. | `anomaly.ts`, `combat.ts`, `world.ts` | escape + lethal-on-second tests; a knowledge test, not a twitch test |
| 9 | **Unclassified input is a free, helpful nudge** — never a silent turn-burn. | `session.ts` | wanderer's nonsense gets a verb-list nudge, no clock advance |
| 10 | **Examining an NPC returns their authored prose**; `deduce` accepts place-nouns + nudges toward the best candidate law. | `render.ts`, `invest.ts` | — |
| 11 | **Dropped items are recoverable** (ground registry); **named destinations** ("go to the holdout") resolve. | `travel.ts`, `assemble.ts`, `parse.ts` | non-metal play is fully reversible |
| 12 | **Bespoke prize prose**; **no-repeat ambient rotation** so hubs stop reading like a metronome. | `travel.ts`, `render.ts` | — |

## Deferred to a future cycle (logged, not dropped)

- **#8 rerollRumorTruth** — the rumor *conflict* surfacing landed (the codex now flags contradictory rumors
  on one topic), but per-seed rumor truth/distortion rerolling (so "who lies" is fresh each seed) is deferred;
  it needs each rumor authored as a truth/distortion pair.
- **Deferred nice-to-haves from 0002** (a "wield" verb to weaponize a law; a second-order return-trip
  interception using the already-modelled stealth path; cross-cutting start-kits; opening the Antenna as a
  genuine alternate corridor; making the Mile Road's doubled-distance bite on the return leg; wiring an
  Exposure/Unsettled threshold). These are depth/replay upside for round 3+.

## State of the game after round 2

Three honest, mechanically-distinct solution paths (fast/stealth-by-day · deduce-and-strip-metal ·
social-buy), four lawful anomalies that all bite *fairly* (warn-before-kill + escape windows), per-seed
variance, a fallible drifting codex, distinct endings, a reachable codex, and a forgiving "Polite-or-better"
parser — all deterministic, realness-verified, and golden-taped. The slice is now a dwell engine, not a
one-route short story.

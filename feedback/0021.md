# feedback/0021 — night16/17 partial validation (API-overload-degraded cohort): the Greywater clock fix is WORKING; re-validate clean when the API recovers

> Source: **cohort-night17-default** — the 12-persona roster against the night16+17 build. **DEGRADED RUN:
> a persistent server-side `API Error: 529 Overloaded` failed 7 of 12 players (they exhausted the launcher's
> 2 retries before playing); only 5 produced clean, realness-verified interviews (all 5 WON, 0 lost).** This
> is NOT a clean validation — treat the signal as directional, and re-run the size-axis merge (default +
> all-opus) once the API is no longer overloaded. The launcher's `failed=7 / retried=10` counters keep the
> degradation honest (no silent drops).

## What the 5 clean players (still) tell us — night17 is reducing the dominant loss mode

night15a's dominant loss mode was the Greywater carry-out as a timing trap (5/7 losses). night17 front-loaded
the deadline (Holt, free) and widened the core night-water window 2→4. Behaviour from the clean players:
- **`saw-grey-deadline`=3** — Holt's new free deadline ("be clear of the bottoms before the water wakes at
  dusk; the core dissolves too, not just iron") is reaching players.
- **`core-recovered`=2 and `core-last-margin`=2** — two players (greywater-breaker, horror-skeptic) hit the
  new rung-3 *last margin* and then **RECOVERED the core** by reaching dry ground — the exact mistimers who
  **LOST in night15a**. The widened window is doing its job: a beeline-out now survives.
- **0 losses among the 5 completers** (vs night15a's 5/7 timing-trap losses). Small/degraded sample, but the
  direction is unambiguous: fewer unfair losses to the clock.
- **No regression:** the night14/15 gate still bites — `used-fast-forward`=3, `clink`=2, `leaned-on-debt`=2,
  parser/prose/hook praised by name.

## Refinements the clean players surfaced (for the next swing)
1. **The widened window can read as TOO soft to a stress-tester** (P2). greywater-breaker: *"the core
   survived even though I stopped to look at rust and a blade and ran deduce… forgiving to the point of not
   actually being a timer."* It is still fatal (a lingerer loses on the 4th beat), but the margin now lets a
   careful-but-stopping player through. This is the intended trade (fewer unfair losses > a tighter timer),
   but if a clean re-validation shows the danger reads toothless, tighten to a 3-beat window or make the
   *meter* sharper (render `core (failing)` at rung 3, distinct from `(unstable)`).
2. **The BOUGHT safe-hour's START is still under-told** (P1, the same family as 0020 #1). horror-skeptic
   paid Mox, mis-sequenced their exploration, reached the pump-house at night, and *"couldn't use what I'd
   paid for… the waste of a coin wasn't [earned]."* Mox names the *deadline* (dusk) but not *"the clock is
   already ticking — go now."* **Fix:** when you buy the safe hour, Mox (or a status beat) should flag that
   daylight is a spending resource ("you've bought the hour, not stopped the sun — get down there while the
   light holds"). Pairs with the night17 telegraph.
3. **A dusk-vs-night credibility gap** (P2). greywater-breaker: iron degraded at **18:56 (dusk)**, which read
   as early against the mental model "after **dark**." It is consistent with the world's own rule ("wakes at
   dusk, call it six") — but the *degradation line* could name the hour ("dusk has woken the water") so the
   timing reads as lawful, not buggy.

## Unexercised this (degraded) cohort — re-validate
- **`distract-gate`=0 — the night16 antenna distraction was not used.** Niche by design (you only keep the
  relic for the gate if you don't trade it to Eun) AND the cohort was both tier-confounded (the
  antenna-field-reachers skew sonnet) and gutted by the 529 failures. Gated-green + the relic's look text now
  telegraphs the dual-use, so it is no longer a dead end — but its in-the-wild use is **unconfirmed**. Route
  a clean cohort (esp. an all-opus pass) to the antenna field to confirm.
- The endgame epilogue staging (0020 #3), the Hollow Dark daylight threat-text leak (0020 #4), and the
  prose-tic de-dup (0020 #8) remain open and were not this cohort's focus.

## Harness / methodology — the API overload is the live bottleneck
- **Do not push cohorts (or the opus compile synth) during a 529 overload** — they fail and waste the budget.
  The hardened launcher's retry/backoff salvaged 5/12 and counted the rest as `failed` (honest), but a 7/12
  failure rate is not a validation. The right move is to ship **gate-validated fixes that don't need
  blind-play** (bug/legibility fixes) until the API recovers, then run the clean size-axis merge.
- **The size-axis de-noise is still owed:** run default + all-opus and merge, so the tier-confound (which
  leaves the antenna-relic and the wait-hint findings single-tier) is resolved, and the night17 "too soft?"
  question is judged across tiers.

## Method note
Even gutted to 5 clean players by a server-side overload, the cohort answered the one question night17 set
out to: the Greywater clock is no longer a trap that punishes a careful player for mistiming — the deadline
is told free and early, and the widened window lets a mistimer recover the prize instead of losing it (the
exact personas who lost in night15a recovered here). The refinements are tuning, not a re-think. A clean
re-validation (default + all-opus, once the API is healthy) is owed to confirm the antenna distraction in the
wild and to check the widened window still reads as a real danger across tiers.
